{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/pcs",
            "args": [],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}